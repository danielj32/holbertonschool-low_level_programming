#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ts1[50];
strcpy(ts1, "\"Programming is like building a multilingual puzzle\n");
puts(ts1);
return (0);
}
