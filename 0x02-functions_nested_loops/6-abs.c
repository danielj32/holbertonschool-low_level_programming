#include "holberton.h"
#include <stdlib.h>
/**
   *_abs - print the lowercase or uppercase.
    *@d: integer
     *Return: d or the 0
      */

int _abs(int d)
{

		if (d < 0)
		{
			return (-d);
		}
	return (d);
}
